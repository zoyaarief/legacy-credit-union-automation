# Optional: enables live OpenAI Responses API decisions for discovery.
# Without it, the app uses the explicit safe simulator at the same provider seam.
OPENAI_API_KEY=
OPENAI_DISCOVERY_MODEL=gpt-5.4-mini
